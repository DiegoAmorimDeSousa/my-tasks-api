{
  "compilerOptions": {
    "baseUrl": "./",
    "paths": {
      // Estes caminhos instruem o 'tsconfig-paths' a procurar os módulos compilados
      // diretamente dentro da pasta 'dist' quando os aliases são encontrados.
      "@domain/*": ["dist/domain/*"],
      "@application/*": ["dist/application/*"],
      "@infrastructure/*": ["dist/infrastructure/*"],
      "@config/*": ["dist/config/*"]
    }
  }
}